diff --git a/src/main/java/br/com/ccortez/drivers/AndroidDriverManager.java b/src/main/java/br/com/ccortez/drivers/AndroidDriverManager.java
index 33c8d67..cff4c30 100644
--- a/src/main/java/br/com/ccortez/drivers/AndroidDriverManager.java
+++ b/src/main/java/br/com/ccortez/drivers/AndroidDriverManager.java
@@ -4,11 +4,14 @@ import br.com.ccortez.config.DriverTestConfig;
 import io.appium.java_client.android.AndroidDriver;
 import io.appium.java_client.android.options.UiAutomator2Options;
 
+import java.net.URI;
 import java.net.MalformedURLException;
 import java.net.URL;
 import java.time.Duration;
 import java.util.Optional;
 
+import org.openqa.selenium.remote.http.ClientConfig;
+
 /**
  * Creates and manages the AndroidDriver used by the UI tests.
  *
@@ -111,12 +114,17 @@ public class AndroidDriverManager {
 
         options.setApp(DriverTestConfig.appiumAppPath());
         options.setCapability("appium:forceAppiumRebuild", true);
+        options.setCapability("appium:showGradleLog", true);
         options.setCapability("appium:espressoBuildConfig", ESPRESSO_BUILD_CONFIG_JSON);
 
         options.setNewCommandTimeout(Duration.ofSeconds(300));
 
-        URL appiumServerUrl = new URL(APPIUM_URL);
-        driver = new AndroidDriver(appiumServerUrl, options);
+        URL validatedServerUrl = new URL(APPIUM_URL);
+        ClientConfig clientConfig = ClientConfig.defaultConfig()
+                .baseUri(URI.create(validatedServerUrl.toString()))
+                .connectionTimeout(Duration.ofMinutes(5))
+                .readTimeout(Duration.ofMinutes(90));
+        driver = new AndroidDriver(clientConfig, options);
 
         // Enable Jetpack Compose element handling in the Espresso driver.
         driver.setSetting("driver", "compose");
